// main.js - Version épurée (25 lignes vs 50+)

document.addEventListener('DOMContentLoaded', () => {
    // Initialiser l'application
    App.init();
    
    // Mode par défaut
    App.setMode('individual');
});

// Raccourcis clavier globaux (déjà gérés dans App.js)
// Sauvegarde automatique (déjà gérée dans App.js)